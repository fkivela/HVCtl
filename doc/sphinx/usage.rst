Usage
=====

HVControl may be launced in an interactive mode by running the script ``main.py``. Alternatively, the HV PSU may be controlled programmatically by creating an instance of the ``api.API`` class and accessing its methods.
